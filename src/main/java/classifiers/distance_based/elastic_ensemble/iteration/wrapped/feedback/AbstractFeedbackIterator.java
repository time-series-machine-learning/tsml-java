package classifiers.distance_based.elastic_ensemble.iteration.wrapped.feedback;

import classifiers.distance_based.elastic_ensemble.iteration.AbstractIterator;
import classifiers.distance_based.elastic_ensemble.iteration.wrapped.AbstractWrappedIterator;

public abstract class AbstractFeedbackIterator<A, B, C> extends AbstractWrappedIterator<A> implements FeedbackIterator<A, B, C> {
}
